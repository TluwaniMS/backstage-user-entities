apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: avenger-users-and-groups-locator
  description: this is a basic main users and groups locator.
spec:
  targets:
    - ./groups/groups.yaml
    - ./users/users.yaml
